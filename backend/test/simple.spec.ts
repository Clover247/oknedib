describe('Simple Math', () => {
  it('should add two numbers correctly', () => {
    expect(2 + 2).toBe(4);
  });

  it('should subtract two numbers correctly', () => {
    expect(5 - 3).toBe(2);
  });
});
